
# Backup configuration.
# Each backup requires a separate configuration file,
# currently multiple files separated by --- (YAML new document) are not supported.

# first backup
destination: dest
sources:
   - src/dir1

